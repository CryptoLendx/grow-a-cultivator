# STATUS — Tahap 0

Satu baris per commit (CLAUDE.md "Cara kerja per sesi" #4). Pemilik menyalin berkas ini ke Project Knowledge.

Lingkungan verifikasi (sesi cloud, tanpa Roblox Studio): rojo 7.4.4 · selene 0.28.0 · stylua 2.0.2 · luau-lsp 1.53.0 · lune 0.8.9. `run-in-roblox` tidak bisa jalan tanpa Studio → test dijalankan headless dengan `lune run tools/testrunner.luau` (subset API TestEZ, pohon Instance tiruan dari `default.project.json`). `wally install` TIDAK bisa diverifikasi di sesi ini (api.wally.run diblokir jaringan); nama paket dicek manual di wally-index.

**Toolchain kini dikunci di `rokit.toml`** (rojo 7.4.4, wally 0.3.2, selene 0.28.0, stylua 2.0.2; pasang: `rokit install`). Paket Wally TIDAK di-vendor ke repo (keputusan pemilik #10); di cloud test tetap lewat runner Lune. Sesi minggu 2 (cloud): biner rilis resmi versi sama + luau-lsp 1.53.0 (definisi `globalTypes.d.luau` tag 1.53.0) + lune 0.8.9; `roblox.yml` selene dibuat dengan selene 0.28.0 yang dikompilasi ulang memakai sertifikat sistem (biner resmi menolak CA proxy sesi), lalu lint dijalankan dengan biner resmi.

**Hasil Studio pemilik (26 Sep 2026 00:08, PC):** `wally install` + `rojo build` + TestEZ 0.4.1 asli via Command Bar → **25 passed, 0 failed, 0 skipped**. Byte hero via `HttpService:JSONEncode` asli = **1.368** (npc lengkap) / **770** (tanpa rl+mm) — identik dengan runner Lune. `Profile.open` belum diuji (butuh DataStore) → ditunda ke 5.4.

| Tugas | Commit | Ringkasan hasil & angka |
|---|---|---|
| 1.1 | wk1: repo | `default.project.json` (src/ReplicatedStorage, src/ServerScriptService, tests→ServerStorage.Tests, Packages/DevPackages/ServerPackages opsional), `wally.toml` (ProfileStore 1.0.3, TestEZ 0.4.1), selene/stylua, `.gitignore`, `CLAUDE.md` = §A verbatim, `tests/run.server.luau`, runner headless `tools/testrunner.luau`. `rojo build -o build/test.rbxl` sukses; `selene src tests` 0 error 0 warning; `stylua --check` bersih; test kosong 1/1 lulus; kontrol negatif (spec sengaja gagal) → exit 1. |
| 1.2 | wk1: shared: Types | `Shared/Types.luau` (--!strict): HeroInput, PartyInput, FloorRow, MissionRequest, MissionResult, EventLine, Appearance (+ tipe bantu), 1 komentar per field — **KOREKSI (wk2):** field `Appearance.tints` sendiri tidak berkomentar (hanya 5 subfield-nya); komentar ditambahkan di `wk1: fix: Types`. `luau-lsp analyze` strict (definisi Roblox + `testez.d.luau`) pada `src` + `tests/*.spec.luau`: 0 error; kontrol negatif (`mode="fast"`, `partyScale` string) tertangkap. Spec contoh lengkap 1/1 lulus. 16 penanda `-- ASUMSI` (nama/struktur tidak eksplisit di INTERFACE §3: tipe id, indeks tint, `MaterialReward`, `missionType`, `enemies`, `Waypoint.point`, seluruh `EventLine`, dll.) → konfirmasi scripter. Catatan: DESAIN_AI_NPC §8.2 memakai `panicEvents` dari MissionResult, tetapi field itu TIDAK ada di §3 → belum ditambahkan. **(wk2: sudah ditambahkan setelah INTERFACE 25 Sep 20:40 — lihat baris `wk1: fix: Types`.)** |
| 1.3 | wk1: shared: Rng | `Shared/Rng.luau` (--!strict): xoshiro128** pure Luau (bit32) dengan API ala `Random` (`NextNumber`, `NextInteger`, `NextUInt32`) + `Rng.derive(seed, ...parts)` → sub-seed 52 bit (2× FNV-1a+fmix32 atas string berpemisah `\31`). Test 7/7: 2 run seed sama → 1.000 angka identik; sub-seed `(profile, hero 1–100, day 1–100, slot 1–10)` = 100.000 sampel, 0 tabrakan; rata-rata NextNumber 10.000 sampel dalam 0,5±0,02; urutan seed 1 dikunci (regresi replay). Keluaran dicocokkan dengan implementasi referensi Python: identik. DEVIASI dari brief: tidak membungkus `Random.new` (algoritma Roblox tidak dijamin tetap lintas versi & tidak tersedia headless) — perlu persetujuan pemilik. Catatan kinerja: ~~100.000 `derive` ≈ 1,2 s di lune~~ **KOREKSI (wk2):** angka 1,2 s adalah durasi seluruh test tabrakan (derive + tostring + tabel set), bukan `derive` saja; dijalankan ulang = **735 ms** (pemilik). Sesi wk2 (Xeon 2,8 GHz, 4 vCPU): test 1.125–1.250 ms, `derive` saja 100.000× = 264–285 ms (3 run) → angka bergantung mesin; pantau terhadap target §9 minggu 4. |
| 1.4 | wk1: core: Profile+Migrate | `Core/Profile.luau`: key `u<userId>_core` / `_heroes_N` (≤1.000) / `_archive_N` (≤2.000) / `_replays` / `_mail` di satu DataStore `PlayerData`, template per jenis ber-`schemaVersion`, pemilihan shard, `heroBytes` (JSONEncode), `Profile.open` (ProfileStore, lazy require). `Core/Migrate.luau`: tabel langkah per jenis key; DUMMY core v1→v2 (+`lastSeen`). Test 7/7: v1→v2 tanpa kehilangan field; versi lebih baru ditolak; data tanpa versi = v1. **Byte hero dummy: npc lengkap (8 rl + 10 mm) = 1.368 byte > 900; tanpa rl+mm = 770 byte ≤ 900** → test hanya meng-assert layout cadangan DESAIN_AI_NPC §1.6; keputusan `social_N` tetap di M1 5.3. Blok rl = 225 B, mm = 361 B (array: 153/201 B) — estimasi §1.6 (≈306 B npc) terlalu rendah. Ukuran diukur dengan encoder JSON tiruan di runner → konfirmasi `HttpService:JSONEncode` di Studio. `Profile.open` BELUM diuji (ProfileStore tidak terpasang). **Studio 26 Sep: 1.368 / 770 byte dikonfirmasi `HttpService:JSONEncode` asli; `Profile.open` ditunda ke 5.4.** |
| 1.5 | wk1: data: Strings | `Data/Strings.luau` (tabel murni, 67 string: 64 [K] + 3 NONKANON) dari DESAIN_AI_NPC §7.3 + DESAIN_SISTEM §5–§6, tiap baris berlabel `[K] ch.N`/`-- NONKANON`; util `Shared/Strings.luau` (`format`, `get`, `placeholders`) dipisah karena Data wajib tanpa logika. Test 5/5: setiap id terisi penuh placeholder-nya; 0 string duplikat (kontrol negatif: duplikat sengaja → gagal); argumen kurang/id asing → error. Cek mekanis: 19/19 string §7.3 identik; semua string tanpa placeholder ada verbatim di DESAIN_SISTEM. TIDAK dimasukkan (teks terpotong "…" di dokumen): The X has been completed…, Manager in charge…, Possible food types…, business report…, party members insufficient…, proposes a mock battle…, greatly disappointed…, Continuous battles tire…, 1st party…; 7 id NONKANON §7.2 (STEAL, PROVOKE, WENT_ALONE, DECLINED_POSITION, BOND_NAMED, WILL_WRITTEN, MEMORIAL_CARD) karena teksnya ada di `claude/Data_Strings_NONKANON.md` yang tidak disalin ke docs/. Konflik id: §7.2 `MENTIONS_DECEASED` vs §7.3 `MENTIONS_DEAD` → dipakai §7.2. |
| 1.6 | wk1: tools: sheet2luau | `docs/sheet_format.md` (format `sheet.json` v1: kanvas 192×256 / 48×64 × 12 frame [FINAL]; slot, depth, kanal tint, rig, pembagian animasi, palet = isi sheet (TERBUKA)), `assets/sheet.example.json` (dummy), `tools/sheet2luau.py` (stdlib Python; validasi + keluaran deterministik bertipe). Keluaran: `Data/AppearanceData.luau` (9 slot, 10 lapisan, 2 rig, 5 palet) & `Data/SpriteData.luau` (2 sprite) dari contoh dummy. Verifikasi: skrip jalan pada contoh (exit 0); `python3 -m unittest tools/test_sheet2luau.py` 16/16 (13 kasus sheet rusak ditolak); keluaran Luau valid: `luau-compile` OK, `luau-lsp analyze` strict 0 error, StyLua bersih, spec pemuatan 4/4 lulus. Seluruh suite: 25/25 lulus (6 spec). |
| 1.1 fix | wk1: fix: CLAUDE.md | Judul → "Grow a Cultivator" (keputusan pemilik #11). `diff` terhadap blok §A `CLAUDE_CODE_Brief_Tahap0.md` (25 Sep 22:50): identik. |
| 1.2 fix | wk1: fix: Types | Komentar `Appearance.tints`; `HeroResult.panicEvents: {PanicEvent}` (`kind: "fear"\|"panic"\|"despair"`, `timeSec`) + `minHpPct`; `HiddenInput.hesitation` (0..1, TERBUKA) — nama field persis INTERFACE 25 Sep 20:40. Penanda `-- ASUMSI` dibiarkan (keputusan #6). `luau-lsp analyze` strict `src` + `tests/*.spec.luau`: 0 error. Kontrol negatif 4/4 tertangkap (tanpa `minHpPct`, tanpa `panicEvents`, tanpa `hesitation`, `kind="scared"`); untuk itu `HeroResult` di spec dideklarasikan terpisah (literal di dalam map `{[HeroId]: HeroResult}` tidak dicek field yang hilang oleh luau-lsp 1.53.0). Suite 25/25. |
| 1.5 fix | wk1: fix: Strings | +9 [K] dari `Data_Strings_K_Lengkap.md` (cek mekanis 9/9 identik) + 38 varian NONKANON `_1.._n` dari `Data_Strings_NONKANON.md` (38/38 identik, `-- NONKANON` per baris). `GRIEF_VISIT`/`MENTIONS_DECEASED` lama = teks varian `_1` → diganti id `_1` (cegah duplikat); `EXTORT` §7.3 (bentuk contoh tanpa bintang) tetap. Total 112 string (73 [K] + 39 NONKANON). **FAIL_LAST_CHANCE:** potongan "1st party…" di DESAIN_SISTEM adalah `[1st party is all on the 2nd floor…]` (§5.1 baris "Lantai waiting room", ch.64–66/105/156/191/267) — BUKAN string ch.167; teks "will be destroyed" tidak ada di DESAIN_SISTEM. FAIL_LAST_CHANCE tetap dimasukkan sebagai [K] ch.167 (loop chances [V1+]); teks utuh "1st party is all on the 2nd floor…" masih belum ada → tidak dimasukkan. Test strings 7/7 (tanpa duplikat, placeholder terisi, kelengkapan varian). Suite 27/27. |
| 2.1 | wk2: data: NpcData + IdioData | `Data/NpcData.luau` (`export type NpcConfig`, --!strict, tabel murni) memuat koefisien DESAIN_AI_NPC §1.2 roll & drift, §1.2a `xp`/`cou_eff`, §1.5 memori, §1.6a `awr`, §2.2 laju, §3.2–3.6 (vul, ambang, ld, sumber/penurun, luka moral, sudden death, likeability), §4.2–4.6 (relasi, Hall, envy, slot/`focus`, implisit, mentor, SIMCA), §5.1–5.2 (bobot, noise, 17 aksi = 16 utama + makan bersama ringan), §6.1–6.11, §6.10 duka, plus §8.1 catch-up & §8.2 `hidden` (angka tuning wajib di Data). `Data/IdioData.luau` (`IdioConfig`): 6 goal + bobot dari sifat (★ bukan variabel), goal kedua 40 %, jumlah per kategori, 3 makanan [K] ch.64, 8 hobi, 6 kebiasaan, 4 keengganan → target TerrainData/EnemyData. Rumus disimpan sebagai data `Linear` (k + Σ koef·var) / `Product`. Label: 488 baris angka `-- TERBUKA` (474 NpcData + 14 IdioData), 2 [K] (dowry 10000G ch.218, 48 jam ch.67), 10 [FINAL], 19 skala/rentang; 0 baris angka tanpa label (cek mekanis). Test `npcdata.spec` 11/11 (684 angka; tanpa fungsi; variabel Linear dikenal; titik ujung rumus = docs, mis. target rel X0→40/X100→100, focus 0,8→0,4, p sudden death 0,03→0,12, fearResistance 1→−0,05). Kontrol negatif 4/4 (fungsi di data, tipe string, salah ketik variabel, salah transkripsi koefisien). Suite 38/38; `luau-lsp` strict 0 error; selene 0/0; StyLua bersih. TERBUKA yang tidak diberi angka di docs (tidak dikarang): ambang "skor leader tinggi" §4.6, nilai awal likeability, besar puncak duka kedua. |
| sync | wk2: docs: alur sync Cowork | `docs/ALUR_SYNC.md` (peran, siklus per sesi, aturan pertanyaan Qn), `docs/KEPUTUSAN.md` (log 12 keputusan pemilik 25 Sep), `docs/laporan/LAPORAN_SESI_2026-09-25_wk2.md` (laporan sesi, Q1–Q11), CLAUDE.md + bagian "Sinkron dengan Claude Cowork" (atas persetujuan pemilik; brief §A perlu disamakan oleh Cowork). Tanpa perubahan kode; suite tetap 38/38. |
| sync | wk2: docs: KEPUTUSAN Q1–Q11 | Balasan LAPORAN 2026-09-25 Q1–Q11 dicatat ke `docs/KEPUTUSAN.md` (11 baris) sebelum tugas baru. Langkah 0 cocok: DESAIN_AI_NPC 26 Sep 00:46, Brief 26 Sep 01:00, Data_Strings_K_Lengkap 26 Sep 00:46 (main 87ef8d0). |
| Q1–Q11 | wk2: data: keputusan Q1–Q11 | **Q8:** `NpcData`/`IdioData` dipindah ke `src/ServerStorage/Data/` (`default.project.json`: ServerStorage `$path` + `Tests`); `rojo sourcemap` → `ServerStorage/Data/{NpcData,IdioData}`, tidak ada lagi di ReplicatedStorage. **Q4:** `likeability.initial = 50`. **Q3:** `groups.leaderFloorPercentile = 80`. **Q11:** +4 [K] ch.65 (`ROOM_HEADCOUNT`, `FLOOR_ASSIGN_PARTY/NAMES/REST`) verbatim (test format 4/4) → 116 string. Komentar Q1/Q2/Q6 diperbarui. Suite 39/39; `rojo build` sukses. |
| 2.2 | wk2: autonomy: Traits | `Autonomy/Traits.luau` (--!strict, murni): `roll(rng, star, {origin, batch, giftCount?})` → 7 dimensi `clamp(round(N(50,15)))` (hanya μ C per ★ = 45/50/55), 1–2 goal berbobot sifat §1.3 (`goalWeights`), idiosinkrasi per kategori IdioData.counts (Q1), `fd={fav,hated}` (Q2), 2 tag §4.6 dari input summon; `drift(traits, {profileSeed, heroId}, kind, rng)` → (Traits baru, event `traitDrift`), Δ ≤ ±5 & clamp 0–100; `encode`/`decode` → `tr` 14 hex, `gl`/`id`/`tg` indeks. **Angka (10.000 roll/★, ★1/2/3/5):** mean 49,68–50,31 (C: 44,88 / 50,10 / 55,19 / 54,88), σ 14,75–15,32, ekstrem <10/>90 = 0,68–0,80 %; cou 1★ vs 3★ selisih <1; goal kedua 40 %±2; frekuensi goal pertama = Σ peluang bobot (±4σ); encode→decode identik 10.000/10.000; field tersimpan 94–108 byte JSON; roll 10.000× ≈ 242 ms (lune). Drift: 2.000 hero × 5 peristiwa besar, |Δ| ≤ 5, hero tidak-plastis 0 geser, porsi plastis ≈ 50 %±5. Test 12/12; kontrol negatif 7/7 tertangkap (μ C, σ, batas drift, fd tertukar, plastisitas, goal ganda, bobot goal); kontrol tipe 1/1. Suite 51/51; luau-lsp strict 0 error; selene 0/0; StyLua bersih. IdioData: komentar "urutan daftar = format simpan". |
| sync | wk2: docs: KEPUTUSAN Q1–Q4 | PR #3 (branch `claude/charming-goodall-6bpd8i`) di-merge ke `main` (4551272, tanpa konflik). Langkah 0 cocok: DESAIN_AI_NPC 26 Sep 01:16, Brief 26 Sep 01:00. Balasan LAPORAN 2026-09-26 Q1–Q4 (semua A) + catatan docs berubah dicatat ke `docs/KEPUTUSAN.md` (5 baris). `Traits.luau` diverifikasi sesuai tanpa perubahan: idiosinkrasi 6–8 (IdioData.counts), keengganan 50 % (hitungan seragam 0–1), plastisitas per hero (`isPlastic` dari profileSeed+heroId), isolasi/stagnasi di luar `BIG_EVENTS`. |
| 2.3 | wk2: data + autonomy: Needs + mental: Stress/Experience/Likeability | `Autonomy/Needs.luau` (--!strict, murni): 5 kebutuhan §2.2, `step(needs, dims, day)`, `apply(gains)`, `nd` 10 hex encode/decode; rel kontinum: target 40 + 0,6·X, decay ×(0,4 + X/100) hanya sampai "lantai sendiri" 100 − target, kehilangan boleh menembus, ikatan intim → ≥40. `Mental/Stress.luau`: `vul` (cou genetik), ambang 90 − 0,3·vul − 0,2·ld, pulih_dasar Product, tarikan kebutuhan, ×(1 + ld/200), 18 sumber/penurun §3.3 (+ kurva combat fatigue ×1,5/×2,5), ld naik ≥1 hari st≥60 / turun ≥7 hari st<40, event `stressBreak`. `Mental/Experience.luau`: seed 0/15/40, habituasi +2/+4 s.d. plafon 40 + 0,6·cou, sensitisasi −3/−6 bila 3 hari sesudah episode tidak semuanya st<50, E≥65 ×1,5 dua arah, `couEff`, `outcome(HeroResult)`. `Mental/Likeability.luau` §3.6. `Autonomy/Formula.luau` (Linear/Product). NpcData: `needs.initial = 50`, `stress.initialSt/initialLd = 0` (ASUMSI). **Angka:** hero X 51–100 tanpa aksi 30 hari → semua need <30; X 50 → rel berhenti di 30 (lantai); 1 hari dipaksa (Δaut 16–17) > 7 hari diabaikan (7–14); X 0–33 × 365 hari min rel ≥ 40 (X 34–35 → 39,6/39 = lantai); 3★ segar − 1★ segar cou_eff = 12; 1★ 10/15 misi → xp 20/30, selisih ke 3★ segar 6/3; sensitisasi −3/−6, dengan pemulihan 0; E≥65: +3 / −4,5 vs +2 / −3; 3★ dipaksa 12 misi panic → xp 4 < 1★ dirawat 24. Test baru 36 (needs 10, stress 12, experience 9, likeability 5); kontrol negatif 15/15 tertangkap. Suite 87/87; luau-lsp strict 0 error (src + test baru); selene 0/0; StyLua bersih; `rojo build` sukses. Kinerja 2.000 hero × 365 hari = 4,0 s (≈5,5 µs/hero-hari; catch-up 21 hari ≈ 0,23 s). |
