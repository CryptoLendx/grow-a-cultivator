# STATUS — Tahap 0

Satu baris per commit (CLAUDE.md "Cara kerja per sesi" #4). Pemilik menyalin berkas ini ke Project Knowledge.

Lingkungan verifikasi (sesi cloud, tanpa Roblox Studio): rojo 7.4.4 · selene 0.28.0 · stylua 2.0.2 · luau-lsp 1.53.0 · lune 0.8.9. `run-in-roblox` tidak bisa jalan tanpa Studio → test dijalankan headless dengan `lune run tools/testrunner.luau` (subset API TestEZ, pohon Instance tiruan dari `default.project.json`). `wally install` TIDAK bisa diverifikasi di sesi ini (api.wally.run diblokir jaringan); nama paket dicek manual di wally-index.

| Tugas | Commit | Ringkasan hasil & angka |
|---|---|---|
| 1.1 | wk1: repo | `default.project.json` (src/ReplicatedStorage, src/ServerScriptService, tests→ServerStorage.Tests, Packages/DevPackages/ServerPackages opsional), `wally.toml` (ProfileStore 1.0.3, TestEZ 0.4.1), selene/stylua, `.gitignore`, `CLAUDE.md` = §A verbatim, `tests/run.server.luau`, runner headless `tools/testrunner.luau`. `rojo build -o build/test.rbxl` sukses; `selene src tests` 0 error 0 warning; `stylua --check` bersih; test kosong 1/1 lulus; kontrol negatif (spec sengaja gagal) → exit 1. |
| 1.2 | wk1: shared: Types | `Shared/Types.luau` (--!strict): HeroInput, PartyInput, FloorRow, MissionRequest, MissionResult, EventLine, Appearance (+ tipe bantu), 1 komentar per field. `luau-lsp analyze` strict (definisi Roblox + `testez.d.luau`) pada `src` + `tests/*.spec.luau`: 0 error; kontrol negatif (`mode="fast"`, `partyScale` string) tertangkap. Spec contoh lengkap 1/1 lulus. 16 penanda `-- ASUMSI` (nama/struktur tidak eksplisit di INTERFACE §3: tipe id, indeks tint, `MaterialReward`, `missionType`, `enemies`, `Waypoint.point`, seluruh `EventLine`, dll.) → konfirmasi scripter. Catatan: DESAIN_AI_NPC §8.2 memakai `panicEvents` dari MissionResult, tetapi field itu TIDAK ada di §3 → belum ditambahkan. |
| 1.3 | wk1: shared: Rng | `Shared/Rng.luau` (--!strict): xoshiro128** pure Luau (bit32) dengan API ala `Random` (`NextNumber`, `NextInteger`, `NextUInt32`) + `Rng.derive(seed, ...parts)` → sub-seed 52 bit (2× FNV-1a+fmix32 atas string berpemisah `\31`). Test 7/7: 2 run seed sama → 1.000 angka identik; sub-seed `(profile, hero 1–100, day 1–100, slot 1–10)` = 100.000 sampel, 0 tabrakan; rata-rata NextNumber 10.000 sampel dalam 0,5±0,02; urutan seed 1 dikunci (regresi replay). Keluaran dicocokkan dengan implementasi referensi Python: identik. DEVIASI dari brief: tidak membungkus `Random.new` (algoritma Roblox tidak dijamin tetap lintas versi & tidak tersedia headless) — perlu persetujuan pemilik. Catatan kinerja: 100.000 `derive` ≈ 1,2 s di lune → pantau terhadap target §9 minggu 4. |
| 1.4 | wk1: core: Profile+Migrate | `Core/Profile.luau`: key `u<userId>_core` / `_heroes_N` (≤1.000) / `_archive_N` (≤2.000) / `_replays` / `_mail` di satu DataStore `PlayerData`, template per jenis ber-`schemaVersion`, pemilihan shard, `heroBytes` (JSONEncode), `Profile.open` (ProfileStore, lazy require). `Core/Migrate.luau`: tabel langkah per jenis key; DUMMY core v1→v2 (+`lastSeen`). Test 7/7: v1→v2 tanpa kehilangan field; versi lebih baru ditolak; data tanpa versi = v1. **Byte hero dummy: npc lengkap (8 rl + 10 mm) = 1.368 byte > 900; tanpa rl+mm = 770 byte ≤ 900** → test hanya meng-assert layout cadangan DESAIN_AI_NPC §1.6; keputusan `social_N` tetap di M1 5.3. Blok rl = 225 B, mm = 361 B (array: 153/201 B) — estimasi §1.6 (≈306 B npc) terlalu rendah. Ukuran diukur dengan encoder JSON tiruan di runner → konfirmasi `HttpService:JSONEncode` di Studio. `Profile.open` BELUM diuji (ProfileStore tidak terpasang). |
| 1.5 | wk1: data: Strings | `Data/Strings.luau` (tabel murni, 67 string: 64 [K] + 3 NONKANON) dari DESAIN_AI_NPC §7.3 + DESAIN_SISTEM §5–§6, tiap baris berlabel `[K] ch.N`/`-- NONKANON`; util `Shared/Strings.luau` (`format`, `get`, `placeholders`) dipisah karena Data wajib tanpa logika. Test 5/5: setiap id terisi penuh placeholder-nya; 0 string duplikat (kontrol negatif: duplikat sengaja → gagal); argumen kurang/id asing → error. Cek mekanis: 19/19 string §7.3 identik; semua string tanpa placeholder ada verbatim di DESAIN_SISTEM. TIDAK dimasukkan (teks terpotong "…" di dokumen): The X has been completed…, Manager in charge…, Possible food types…, business report…, party members insufficient…, proposes a mock battle…, greatly disappointed…, Continuous battles tire…, 1st party…; 7 id NONKANON §7.2 (STEAL, PROVOKE, WENT_ALONE, DECLINED_POSITION, BOND_NAMED, WILL_WRITTEN, MEMORIAL_CARD) karena teksnya ada di `claude/Data_Strings_NONKANON.md` yang tidak disalin ke docs/. Konflik id: §7.2 `MENTIONS_DECEASED` vs §7.3 `MENTIONS_DEAD` → dipakai §7.2. |
